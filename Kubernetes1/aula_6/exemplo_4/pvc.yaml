apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fiap-pvc-mongo
  labels:
    name: fiap-pvc-mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 
  storageClassName: local-storage-db